# 🏗️ REWARDS SYSTEM - ARCHITECTURE OVERVIEW

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    WATERCARE PORTAL                         │
│                   Rewards System v1.0                       │
└─────────────────────────────────────────────────────────────┘

                          ┌────────────┐
                          │   INDEX    │
                          │  (Home)    │
                          │  HTML      │
                          └──────┬─────┘
                                 │
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
          ┌────────────┐   ┌────────────┐   ┌────────────┐
          │  REPORTS   │   │SUGGESTIONS │   │  REWARDS   │
          │   PAGE     │   │    PAGE    │   │    PAGE    │
          │   HTML     │   │    HTML    │   │   HTML ★   │
          └─────┬──────┘   └─────┬──────┘   └──────┬─────┘
                │                │                 │
                └────────────────┼─────────────────┘
                                 │
                      ┌──────────▼──────────┐
                      │   JavaScript       │
                      │  Points System     │
                      │  (rewards.js) ★    │
                      │                    │
                      │ ┌────────────────┐ │
                      │ │ Points Logic   │ │
                      │ │ Badge System   │ │
                      │ │ Level Progress │ │
                      │ │ Leaderboard    │ │
                      │ └────────────────┘ │
                      └──────────┬─────────┘
                                 │
                      ┌──────────▼──────────┐
                      │  Browser Storage   │
                      │  localStorage      │
                      │                    │
                      │ watercare_user {   │
                      │  name, points,     │
                      │  reports, etc.     │
                      │ }                  │
                      └────────────────────┘

★ = New/Enhanced Components
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER ACTIONS                             │
└─────────────────────────────────────────────────────────────┘

User Submits Report          User Clicks Button       User Updates Name
         │                          │                        │
         ▼                          ▼                        ▼
┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
│ Trigger Events   │      │ Click Handlers   │      │ Form Submission  │
│ (form submit)    │      │ (inline JS)      │      │ (modal)          │
└────────┬─────────┘      └────────┬─────────┘      └────────┬─────────┘
         │                         │                         │
         ▼                         ▼                         ▼
┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐
│ addReport()      │      │ addSession()     │      │ saveUserProfile()│
│ addSuggestion()  │      │ addCleanup()     │      │                  │
└────────┬─────────┘      └────────┬─────────┘      └────────┬─────────┘
         │                         │                         │
         └─────────────┬───────────┴─────────────┬───────────┘
                       │                         │
                       ▼                         ▼
              ┌────────────────────┐   ┌────────────────────┐
              │ Update User Data   │   │ Save to localStorage
              │ • Add Points       │   │ • Name saved
              │ • Increment Count  │   │ • Profile updated
              └────────┬───────────┘   └────────┬───────────┘
                       │                         │
                       └─────────────┬───────────┘
                                     │
                                     ▼
                        ┌────────────────────────┐
                        │  Check Badges & Level │
                        │  Update UI Elements   │
                        │  Show Notifications  │
                        └────────────────────────┘
```

---

## Component Structure

```
┌─ REWARDS SYSTEM ─────────────────────────────────────────┐
│                                                            │
│  ┌─ USER PROFILE CARD ─────────────────────────────────┐ │
│  │ • Avatar                                            │ │
│  │ • User Name                                         │ │
│  │ • Current Level                                     │ │
│  │ • Total Points Display                              │ │
│  │ • Activity Breakdown                                │ │
│  │ • Progress Bar                                      │ │
│  │ • Update Profile Button                             │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌─ EARNING GUIDE ──────────────────────────────────────┐ │
│  │ • Report Issue (+25)                                │ │
│  │ • Attend Session (+15)                              │ │
│  │ • Cleanup (+20)                                     │ │
│  │ • Share Ideas (+10)                                 │ │
│  │ • Milestones (BONUS)                                │ │
│  │ • Referral (+50)                                    │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌─ BADGES SECTION ─────────────────────────────────────┐ │
│  │ [📝] [👥] [🧹] [♻️] [💧] [🏆]                         │ │
│  │ First Session Cleanup Eco  Water Champion           │ │
│  │        Warrior             Guardian                 │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌─ LEADERBOARD ────────────────────────────────────────┐ │
│  │ Rank │ Name    │ Pts │ Reports │ Sessions │ Badge   │ │
│  │ 🥇 1 │ Raj... │ 580 │   8    │   12    │ 🏆     │ │
│  │ 🥈 2 │ Priya  │ 420 │   6    │   10    │ 💧     │ │
│  │ 🥉 3 │ Aditya │ 350 │   5    │    8    │ ♻️     │ │
│  │ ... (more)                                          │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌─ LEVELS CARDS ───────────────────────────────────────┐ │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐    │ │
│  │ │Contrib. │ │Eco War. │ │Guardian │ │Champion │    │ │
│  │ │0-100pts │ │101-250  │ │251-500  │ │500+ pts │    │ │
│  │ └─────────┘ └─────────┘ └─────────┘ └─────────┘    │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌─ QUICK ACTIONS ──────────────────────────────────────┐ │
│  │ [📝 Report] [👥 Attend] [🧹 Cleanup]                │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## JavaScript Module Structure

```
rewards.js
│
├─ Data Storage
│  ├─ mockUsers[]           (Leaderboard template)
│  ├─ POINTS_CONFIG         (25, 15, 20, 10, 50)
│  ├─ LEVEL_CONFIG          (0, 101, 251, 501)
│  └─ BADGE_CONFIG          (6 badge thresholds)
│
├─ User Management
│  ├─ initializeUserData()
│  ├─ getUserData()
│  ├─ saveUserData()
│  └─ updateUI()
│
├─ Points System
│  ├─ addReport()          (+25)
│  ├─ addSession()         (+15)
│  ├─ addCleanup()         (+20)
│  └─ addSuggestion()      (+10)
│
├─ Level System
│  ├─ getUserLevel()
│  ├─ getCurrentLevelThreshold()
│  └─ getNextLevelThreshold()
│
├─ Badge System
│  ├─ checkBadges()
│  ├─ countUnlockedBadges()
│  └─ BADGE_CONFIG checking
│
├─ UI Updates
│  ├─ updateProgressBar()
│  ├─ updateLeaderboard()
│  ├─ showNotification()
│  └─ Event listeners
│
└─ Modal & Forms
   ├─ showUserInputModal()
   ├─ closeUserModal()
   └─ saveUserProfile()
```

---

## CSS Module Structure

```
styles.css (Rewards Section Additions)

├─ Rewards Container
│  ├─ rewards-section
│  ├─ rewards-container
│  └─ responsive grid
│
├─ Profile Card
│  ├─ user-profile-card
│  ├─ profile-header
│  ├─ points-display
│  ├─ progress-bar-section
│  └─ update-profile-btn
│
├─ Earning Guide
│  ├─ earning-guide
│  ├─ earning-items
│  ├─ earning-item
│  └─ points-badge
│
├─ Badges
│  ├─ achievements-section
│  ├─ badges-container
│  ├─ badge
│  └─ badge.unlocked
│
├─ Leaderboard
│  ├─ leaderboard-section
│  ├─ leaderboard-table
│  └─ responsive styling
│
├─ Levels
│  ├─ levels-section
│  ├─ level-card
│  └─ level variants
│
└─ Quick Actions
   ├─ quick-actions-section
   ├─ actions-grid
   └─ action-btn styling
```

---

## Integration Points

```
┌──────────────────────────────────────────────────────────┐
│           INTEGRATION WITH EXISTING PAGES                 │
└──────────────────────────────────────────────────────────┘

1. NAVBAR (index.html)
   ├─ Added 🏆 Rewards link
   └─ Points to rewards.html

2. REPORT FORM (report.html)
   ├─ leakForm.addEventListener('submit')
   ├─ Calls → addReport()
   ├─ Awards → 25 points
   └─ Shows → Success message with points

3. SUGGESTIONS FORM (suggestions.html)
   ├─ suggestionForm.addEventListener('submit')
   ├─ Calls → addSuggestion()
   ├─ Awards → 10 points
   └─ Shows → Success message with points

4. CSS STYLING (css/styles.css)
   ├─ 400+ new lines
   ├─ Rewards page styling
   ├─ Responsive design
   └─ Mobile optimizations

5. JavaScript (js/app.js)
   ├─ Updated form handlers
   ├─ Points award integration
   └─ Success notifications
```

---

## Data Flow Example: Report Submission

```
┌────────────────────────────────────────────────────────────┐
│ User submits Report Form on report.html                   │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ Form submission event triggered                           │
│ → stopPropagation()                                       │
│ → validateForm()                                          │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ Check: window.WaterCareRewards exists?                    │
└────────────────────────────────────────────────────────────┘
                          ▼ YES
┌────────────────────────────────────────────────────────────┐
│ window.WaterCareRewards.addReport() called                │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ rewards.js → addReport() function                         │
│ 1. Get user from localStorage                             │
│ 2. Add 25 points                                          │
│ 3. Increment reports count                                │
│ 4. Call saveUserData()                                    │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ saveUserData() function                                    │
│ 1. Save to localStorage                                   │
│ 2. Call updateUI()                                        │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ updateUI() function                                        │
│ 1. Display points (report.html hidden)                    │
│ 2. Check badges (updates badges if next page visited)    │
│ 3. Calculate level                                        │
│ 4. Update progress bar                                    │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ showNotification() function                                │
│ Shows: "Report submitted! +25 points earned!"            │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ User sees success message & points in alert               │
└────────────────────────────────────────────────────────────┘
                          ▼
┌────────────────────────────────────────────────────────────┐
│ User navigates to rewards.html                            │
│ → All data displays correctly                             │
│ → Points updated                                          │
│ → Badges check for unlocks                                │
│ → Leaderboard updates                                     │
└────────────────────────────────────────────────────────────┘
```

---

## State Management

```
┌─────────────────────────────────────────────────────┐
│              STATE OBJECT STRUCTURE                 │
└─────────────────────────────────────────────────────┘

localStorage.watercare_user = {
  name: String,              ← User's name
  points: Number,            ← Total points earned
  reports: Number,           ← Reports submitted
  sessions: Number,          ← Sessions attended
  cleanup: Number,           ← Cleanups participated
  suggestions: Number        ← Suggestions shared
}

Derived States (Calculated):
├─ level = getLevelFromPoints(points)
├─ nextLevelThreshold = getNextThreshold(points)
├─ progressPercent = calculateProgress(points)
├─ unlockedBadges = checkBadges(state)
└─ leaderboardRank = findRank(state, allUsers)
```

---

## Responsive Breakpoints

```
┌─────────────────────────────────────────────────────┐
│         DEVICE SIZE ADAPTATIONS                     │
└─────────────────────────────────────────────────────┘

Mobile (< 600px)
├─ Single column layout
├─ Full-width cards
├─ Stacked badge grid (1 col)
├─ Simplified leaderboard
└─ Touch-friendly buttons

Tablet (600px - 1000px)
├─ Two column layout
├─ Side-by-side sections
├─ 2-column badge grid
├─ Full leaderboard
└─ Comfortable spacing

Desktop (> 1000px)
├─ Multi-column layout
├─ Full UI with sidebars
├─ 3-column badge grid
├─ Complete leaderboard
└─ Large typography
```

---

## Error Handling

```
Error Scenarios Handled:
├─ localStorage unavailable
│  └─ Graceful fallback to in-memory storage
│
├─ Missing user data
│  └─ Initialize with defaults
│
├─ Invalid form data
│  └─ Validate before saving
│
├─ Concurrent updates
│  └─ Last write wins (setTimeout safety)
│
└─ Modal interaction
   └─ Close on outside click
   └─ Close on Escape key
```

---

## Performance Optimization

```
✅ Optimizations Implemented:

1. Minimal DOM Operations
   └─ Update only what changed

2. Event Delegation
   └─ Single listener for multiple items

3. CSS Animations
   └─ GPU-accelerated transitions

4. Efficient Sorting
   └─ leaderboard sorted once per update

5. localStorage Caching
   └─ Single parse operation

6. No External API Calls
   └─ All calculations local

7. Lazy Loading Ready
   └─ Code can add image lazy-load

Results:
├─ Fast page load
├─ Smooth interactions
├─ Responsive UI
└─ No lag or jank
```

---

## Version History

```
v1.0 - Initial Release (2025-11-18)
├─ Points system
├─ Badge system
├─ Level progression
├─ Leaderboard
├─ Data persistence
├─ Full documentation
└─ Production ready
```

---

**Architecture Version**: 1.0  
**Design Pattern**: State Management + Event-Driven  
**Technology**: Vanilla JS + CSS3 + HTML5  
**Status**: ✅ Complete & Optimized
